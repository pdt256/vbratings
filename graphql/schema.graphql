schema {
    query: Query
}

type Query {
    topPlayers(year: Int, gender: String, limit: Int): [PlayerRating]!
}

type PlayerRating {
    rating: Int!
    playerName: String!
    totalMatches: Int!
}
